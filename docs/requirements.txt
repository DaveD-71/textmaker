python-docx>=0.8.11
pyyaml>=6.0
click>=8.0
python-pptx>=0.6.23

# Required external downloads for audio generation (not pip-installable):
# - Install the Piper binary and make `piper` available on PATH.
# - Install `ffmpeg` and make `ffmpeg` available on PATH (used for MP3 encoding).
# - Download each model file (.onnx) and matching config (.onnx.json) into
#   `textmaker/piper_voices/`.
# - These are required by `yaml-to-audio` and `generate_roleplay_audio`.
#
# Required model files (current and future):
# - Any `.onnx` + `.onnx.json` referenced by your active voice-map JSON files.
