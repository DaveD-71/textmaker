Clean flat-vector educational diagram style, muted professional color palette (blues, grays, one warm accent color), no photographic texture, no gradients, generous white space, thin consistent line weights, simple geometric icons, textbook-infographic quality. No readable body text beyond short labels explicitly specified in the prompt. No logos, no watermarks.

Specific illustration to create:
A clean, modern, professionally designed flat-vector infographic component showing a single continuous vertical stack of EXACTLY EIGHT separate rounded-rectangle boxes -- count them: box 1, box 2, box 3, box 4, box 5, box 6, box 7, box 8, each one fully separate from its neighbors with a visible gap and its own connecting arrow, never two icons sharing one box. Transparent background (no background at all, artwork only). All eight boxes are exactly the same height and width, with equal, consistent vertical spacing between every consecutive pair. Each box contains one well-designed, polished icon (not generic clip-art -- professional published-textbook quality) positioned in the left quarter of the box, sized proportionate to the box height (roughly 55-65% of it). Leave the right three-quarters of each box as clean, intentionally-designed empty space (a subtle inner shadow or soft fill tint is fine) reserved for a text label to be added afterward. The eight icons, one per box, top to bottom, each in its OWN separate box: box 1 = a target/bullseye. box 2 = a speech-bubble containing a small star. box 3 = a document with horizontal text lines. box 4 = three stacked/overlapping building blocks. box 5 = a presentation slide or bar chart. box 6 = a pencil (this is its own box, separate from box 7). box 7 = a pair of scissors (this is its own box, separate from box 6 and box 8 -- do not merge this with the pencil box). box 8 = a person mid-speech with a small sound/speech indicator. Add a colored vertical accent bar along the left edge of the whole stack, in three colored segments matching this exact grouping: color A runs alongside boxes 1-2-3-4-5 (five boxes), color B runs alongside boxes 6-7 (two boxes, both amber, clearly two box-heights tall, not one), color C runs alongside box 8 alone (one box). Verify before finalizing: there must be visibly 8 distinct boxes with 7 connecting arrows between them, and the amber color B segment must span two full box-heights (boxes 6 and 7), not one. All three colors from the same muted professional palette, clearly distinguishable, with blank margin to their left for phase-name text added afterward. Absolutely no text, letters, numbers, or words anywhere in the image. High production quality, suitable for a professionally published textbook.
